#include <stdio.h>
#include <string.h>

int main()
{
    int N, M;
    char N_num[500000][21], M_num[500000][21];
    scanf("%d %d", &N, &M);

    for(int i = 0; i < N; i++)
    {
        scanf("%s", &N_num[i]);
    }

    for(int i = 0; i < M; i++)
    {
        scanf("%s", &M_num);
    }

    


    return 0;
}