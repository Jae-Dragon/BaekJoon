#include <stdio.h>

int main()
{
    int N, Arr[50] = {0}, cnt = 0;
    scanf("%d", &N);

    for(int i = 0; i < N; i++)
    {
        int M;
        scanf("%d", &M);
        Arr[cnt++] = M;
    }

    for(int i = 0; i < N; i++)
    {
        for(int j = 2;;j++){
            if()
        }
    }
    return 0;
}